lib3mf for Debian

The upstream library embeds the libzip and zlib libraries.
Both are removed from the Debian build.  They are still included in
the pkgconfig control file, as with upstream, but in the Debian build
they are also included in the library's runtime dependencies.

 -- Joel Ray Holveck <joelh@piquan.org>  Fri, 25 Sep 2020 20:20:30 -0700
